# 📊 Dashboard Project

Аналитический дашборд для мониторинга складских операций и работы сотрудников.

Проект разработан на **Python + Streamlit** и предназначен для получения данных из PostgreSQL, отображения аналитических отчетов, поиска по результатам, выгрузки данных в Excel, мониторинга текущих складских процессов и сбора предложений по развитию дашборда.

---

## 🚀 Возможности

### 📊 Аналитические отчеты

Дашборд позволяет выбирать и одновременно отображать несколько аналитических отчетов.

В проекте доступны отчеты:

* Не выданные заявки
* Неотобранные артикулы с разбивкой на подтипы
* Статистика по отобранным и неотобранным артикулам
* Статусы заявок отгрузки
* Количество отборов сотрудников по волнам
* Отборы сотрудников по волнам
* Средняя скорость отбора в час
* Выданные клиентам заказы
* Операции по ресурсам

Список отчетов хранится централизованно в файле `queries.py`.

---

## 🔎 Поиск по таблицам

Для отчетов с большим количеством данных предусмотрен встроенный поиск.

Поиск выполняется **сразу по всем столбцам таблицы**.

Доступны следующие функции:

* 🔎 поиск по таблице;
* ⌨️ запуск поиска клавишей `Enter`;
* 🔎 запуск поиска кнопкой **«Найти»**;
* ✖️ очистка поискового запроса;
* отображение только найденных строк;
* поиск без изменения исходных данных отчета.

Поиск выполняется по текстовому представлению значений всех столбцов, поэтому можно искать как по текстовым полям, так и по числовым значениям, датам и другим данным.

---

## 📥 Выгрузка в Excel

Результаты отчетов можно скачать непосредственно из интерфейса дашборда.

Для каждого отчета доступна кнопка:

**⇩ Скачать**

Данные выгружаются в формате:

```text
.xlsx
```

Для создания Excel-файла используется библиотека **OpenPyXL**.

### Экспорт учитывает поиск

Если перед скачиванием был выполнен поиск, в Excel попадут **только найденные строки**.

Например:

1. открыть отчет;
2. ввести значение в поле поиска;
3. нажать **«Найти»**;
4. проверить найденные строки;
5. нажать **«⇩ Скачать»**.

В результате будет сформирован Excel-файл только с отфильтрованными данными.

Если поиск не выполнялся, в Excel выгружается полный результат отчета.

---

## 🛠️ Разработка и предложения

В проекте предусмотрена отдельная страница **«Разработка»** для сбора предложений пользователей по развитию аналитического дашборда.

Пользователь может предложить:

* новый отчет;
* изменение существующего отчета;
* улучшение интерфейса;
* исправление найденной проблемы;
* новую функциональность;
* другое изменение, связанное с развитием проекта.

### 💡 Добавление предложения

Для добавления предложения используется кнопка:

**➕ Предложить изменение**

Открывается всплывающее окно, в котором необходимо указать:

* **тему или краткое описание**;
* **имя автора**;
* **текст предложения**.

Также к предложению можно прикрепить файлы.

### 📎 Вложения

Поддерживается прикрепление нескольких файлов к одному предложению.

Можно добавлять, например:

* скриншоты `PNG`, `JPG`, `JPEG`, `WEBP`, `GIF`;
* PDF-файлы;
* Excel-файлы;
* Word-документы;
* текстовые файлы;
* CSV-файлы.

Изображения отображаются непосредственно в предложении, остальные файлы доступны для скачивания.

Вложения сохраняются локально в каталоге:

```text
logs/suggestions/
```

### 📋 Статусы предложений

Каждое предложение имеет один из двух статусов:

* 🟡 **В работе**
* 🟢 **Выполнено**

На странице **«Разработка»** отображается статистика:

* 💡 Всего предложений;
* 🟡 В работе;
* 🟢 Выполнено.

Также доступна фильтрация предложений по статусу.

### 📖 Просмотр предложений

Предложения отображаются в свернутом виде.

В списке видны:

* тема предложения;
* автор;
* текущий статус.

При нажатии на предложение оно раскрывается и показывает:

* дату создания;
* подробное описание;
* вложения;
* информацию о выполнении.

Это позволяет сохранять страницу компактной даже при большом количестве предложений.

---

## 🔐 Административная панель

В проекте предусмотрена отдельная административная страница.

Доступ к странице защищен паролем.

Административная панель позволяет просматривать статистику использования дашборда и управлять предложениями пользователей.

### 📈 Статистика использования

В административной панели отображаются:

* количество уникальных посетителей;
* общее количество действий;
* количество сессий;
* количество действий за текущий день;
* популярные отчеты;
* ежедневная активность;
* последние действия пользователей;
* IP-адреса пользователей.

### 💡 Управление предложениями

В административной панели отображаются все предложения пользователей.

Администратор может:

* просматривать тему предложения;
* видеть автора;
* просматривать подробное описание;
* открывать вложения;
* скачивать прикрепленные файлы;
* устанавливать статус **«Выполнено»**;
* возвращать предложение в статус **«В работе»**;
* удалять предложения.

При установке статуса **«Выполнено»** сохраняются:

* дата выполнения;
* пользователь, выполнивший изменение статуса.

---

## 📈 Визуализация

Для некоторых отчетов предусмотрено автоматическое построение графиков.

Используется библиотека **Plotly**.

Доступны:

* столбчатые диаграммы;
* линейные графики для временных данных.

Графики можно включать и отключать через настройки боковой панели.

---

## 📋 Отображение нескольких отчетов

Дашборд поддерживает одновременный просмотр нескольких отчетов.

В боковой панели можно выбрать:

* один отчет;
* несколько отчетов.

Также доступен выбор расположения:

* **в одну колонку**;
* **в две колонки**.

Это позволяет одновременно сравнивать результаты нескольких отчетов.

---

## ⚙️ Настройки отображения

В боковой панели доступны настройки:

* расположение отчетов в одну или две колонки;
* показ/скрытие графиков;
* показ/скрытие сводной статистики;
* показ/скрытие таблиц с данными.

---

## 🔄 Обновление данных

Данные загружаются из **PostgreSQL**.

Для ускорения работы используется кеширование Streamlit.

В боковой панели доступна кнопка:

**🔄 Обновить данные**

При её нажатии:

1. кеш данных очищается;
2. приложение перезапускает отображение;
3. данные повторно загружаются из PostgreSQL.

---

## 🔥 Часто используемые отчеты

На главной странице отображаются наиболее часто запускаемые отчеты.

Для каждого отчета показывается:

* название;
* количество запусков;
* кнопка быстрого запуска.

Статистика формируется на основании действий пользователей и хранится в локальной SQLite-базе.

Это позволяет быстро запускать наиболее востребованные отчеты без необходимости искать их в списке.

---

## 📝 Логирование

В проекте реализовано логирование действий пользователей.

Для хранения логов используется **SQLite**.

Логируются:

* посещения дашборда;
* просмотры отчетов;
* параметры запросов;
* время действия;
* User-Agent;
* session ID;
* IP-адрес, если он доступен приложению.

### Идентификация посетителей

Для подсчета уникальных посетителей используется `session_id`.

Это позволяет корректнее считать пользователей, чем использование только IP-адреса, поскольку несколько пользователей могут работать через один IP-адрес.

Файл базы логов:

```text
logs/dashboard_logs.db
```

---

# 🛠 Технологии

| Технология    | Назначение                            |
| ------------- | ------------------------------------- |
| Python        | Основной язык                         |
| Streamlit     | Веб-интерфейс                         |
| PostgreSQL    | Основная база данных                  |
| SQLAlchemy    | Подключение к PostgreSQL              |
| psycopg2      | PostgreSQL driver                     |
| Pandas        | Обработка данных                      |
| Plotly        | Визуализация                          |
| SQLite        | Логирование, статистика и предложения |
| OpenPyXL      | Создание Excel-файлов                 |
| python-dotenv | Работа с переменными окружения        |

---

# 📁 Структура проекта

```text
dashboard_project/
│
├── app.py
├── config.py
├── queries.py
├── logger.py
├── suggestions.py
├── run_server.py
│
├── pages/
│   ├── admin.py
│   ├── Дашборд.py
│   ├── Разработка.py
│   └── Выдача клиенту.py
│
├── requirements.txt
├── README.md
│
├── .streamlit/
│   └── config.toml
│
├── logs/
│   ├── dashboard_logs.db
│   └── suggestions/
│       └── ...
│
└── ...
```

### Основные файлы

#### `app.py`

Главный файл приложения.

Отвечает за:

* интерфейс;
* выбор отчетов;
* параметры запросов;
* отображение таблиц;
* поиск;
* фильтрацию;
* построение графиков;
* сводную статистику;
* экспорт данных в Excel;
* взаимодействие с пользователем;
* отображение часто используемых отчетов.

#### `queries.py`

Содержит SQL-запросы проекта.

Запросы организованы в словаре и вызываются из `app.py` по названию отчета.

Это позволяет отделить SQL-логику от интерфейса приложения.

#### `config.py`

Содержит настройки подключения к PostgreSQL.

Параметры подключения берутся из `.env`.

#### `logger.py`

Отвечает за:

* создание SQLite-базы логов;
* запись действий пользователей;
* хранение пользовательских сессий;
* получение статистики использования отчетов.

#### `suggestions.py`

Отвечает за работу с предложениями по развитию проекта.

Модуль обеспечивает:

* создание предложений;
* хранение предложений в SQLite;
* получение предложений;
* изменение статуса;
* удаление предложений;
* работу с вложениями;
* получение статистики предложений.

#### `pages/`

Содержит дополнительные страницы Streamlit-приложения.

Основные страницы:

* `admin.py` — административная панель и статистика;
* `Разработка.py` — предложения и задачи по развитию;
* `Выдача клиенту.py` — работа с выдачей клиентам.

#### `.streamlit/config.toml`

Содержит настройки Streamlit и оформления интерфейса.

---

# 🔐 Настройка подключения к PostgreSQL

Параметры подключения рекомендуется хранить в `.env`.

Пример:

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_password
```

> Файл `.env` не должен публиковаться в GitHub.

Добавьте его в `.gitignore`:

```text
.env
```

---

# 📦 Установка

## 1. Клонирование репозитория

```bash
git clone https://github.com/romaapp/dashboard_project.git
```

Перейдите в каталог проекта:

```bash
cd dashboard_project
```

---

## 2. Создание виртуального окружения

Windows:

```bash
python -m venv .venv
```

Активация:

```bash
.venv\Scripts\activate
```

---

## 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

Основные зависимости проекта:

```text
streamlit
pandas
plotly
SQLAlchemy
psycopg2-binary
openpyxl
python-dotenv
```

---

# 🗄 Настройка базы данных

Проект использует **PostgreSQL** в качестве основной базы данных.

Перед запуском необходимо:

1. установить PostgreSQL;
2. создать базу данных;
3. предоставить пользователю необходимые права;
4. указать параметры подключения в `.env`.

Структура таблиц и необходимые SQL-запросы зависят от используемой WMS-системы.

Дополнительные данные приложения, включая логи, статистику и предложения пользователей, хранятся в локальной **SQLite-базе**.

---

# ▶️ Запуск

После настройки окружения выполните:

```bash
streamlit run app.py
```

После запуска Streamlit предоставит адрес приложения, например:

```text
http://localhost:8501
```

Откройте его в браузере.

---

# 📊 Работа с отчетами

После запуска:

1. выберите необходимые отчеты в боковой панели;
2. при необходимости задайте параметры;
3. выберите настройки отображения;
4. просмотрите результаты;
5. используйте поиск для фильтрации данных;
6. при необходимости скачайте результат в Excel.

---

# 📋 Параметризованные отчеты

Некоторые отчеты требуют дополнительных параметров.

### Статусы заявок отгрузки

Для отчета необходимо указать номер заявки.

Пример:

```text
00-00000018
```

### Выданные клиентам заказы

Для отчета необходимо выбрать:

* начальную дату;
* конечную дату.

---

# 🧹 Кеширование

Для ускорения работы используется механизм кеширования Streamlit:

```python
@st.cache_data
```

Подключение к PostgreSQL кешируется отдельно:

```python
@st.cache_resource
```

При необходимости кеш данных можно очистить кнопкой:

**🔄 Обновить данные**

---

# 📌 Особенности проекта

* Интерфейс построен на Streamlit.
* SQL-запросы отделены от основной логики приложения.
* Подключение к PostgreSQL вынесено в отдельный модуль.
* Логирование вынесено в отдельный модуль.
* Используется SQLite для хранения статистики использования и предложений.
* Реализован поиск по всем столбцам таблицы.
* Поиск можно запускать клавишей `Enter`.
* Реализована кнопка очистки поиска.
* Реализована фильтрация результатов.
* Реализован экспорт результатов в Excel.
* Excel содержит именно текущий результат фильтрации.
* Поддерживается отображение нескольких отчетов одновременно.
* Поддерживается отображение отчетов в одну или две колонки.
* Для некоторых отчетов доступны графики.
* Для некоторых отчетов доступна сводная статистика.
* Реализованы плитки часто используемых отчетов.
* Реализовано логирование действий пользователей.
* Используется кеширование данных для ускорения работы.
* Добавлена отдельная страница **«Разработка»**.
* Пользователи могут отправлять предложения по развитию дашборда.
* К предложениям можно прикреплять несколько файлов.
* Изображения отображаются непосредственно в предложениях.
* Реализованы статусы **«В работе»** и **«Выполнено»**.
* Добавлена статистика предложений.
* Предложения отображаются в раскрывающихся блоках.
* Администратор может изменять статус и удалять предложения.
* Администратор может просматривать и скачивать вложения.

---

# 🔒 Безопасность

Не рекомендуется хранить в репозитории:

* пароли от PostgreSQL;
* `.env`;
* персональные данные пользователей;
* production-базы данных;
* реальные логи пользователей;
* загруженные пользователями файлы.

Перед публикацией проекта убедитесь, что `.gitignore` содержит:

```text
.env
.venv/
__pycache__/
*.pyc
logs/
*.db
*.sqlite3
```

---

# 📄 Лицензия

Проект предназначен для учебного и внутреннего использования.

При необходимости лицензию можно изменить на подходящую для конкретного способа распространения проекта.

---

# 👨‍💻 Автор

**Mishin Roman**

GitHub:

https://github.com/romaapp
